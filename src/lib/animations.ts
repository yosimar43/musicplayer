/**
 * 🎨 Sistema de animaciones - Anime.js eliminado
 * Funciones placeholder para mantener compatibilidad
 */

/**
 * 💫 Fade In - Placeholder (sin animación)
 */
export const fadeIn = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🔍 Scale In - Placeholder (sin animación)
 */
export const scaleIn = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 📋 Stagger Items - Placeholder (sin animación)
 */
export const staggerItems = (
  selector: string,
  options?: { delay?: number; staggerDelay?: number }
) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * ✨ Slide In from Left - Placeholder (sin animación)
 */
export const slideInLeft = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * ➡️ Slide In from Right - Placeholder (sin animación)
 */
export const slideInRight = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🎵 Pulse - Placeholder (sin animación)
 */
export const pulse = (selector: string, options?: { scale?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🌟 Glow - Placeholder (sin animación)
 */
export const glow = (selector: string) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🎨 Rotate In - Placeholder (sin animación)
 */
export const rotateIn = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🎲 Random Float - Placeholder (sin animación)
 */
export const randomFloat = (selector: string) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🌊 Wave - Placeholder (sin animación)
 */
export const wave = (selector: string, index: number) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 💥 Pop In - Placeholder (sin animación)
 */
export const popIn = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🔄 Spin - Placeholder (sin animación)
 */
export const spin = (selector: string, options?: { duration?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 📊 Progress Bar - Placeholder (sin animación)
 */
export const progressBar = (selector: string, progress: number) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🎯 Bounce - Placeholder (sin animación)
 */
export const bounce = (selector: string) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🌈 Color Shift - Placeholder (sin animación)
 */
export const colorShift = (selector: string) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 📤 Slide Up - Placeholder (sin animación)
 */
export const slideUp = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🎪 Zoom In - Placeholder (sin animación)
 */
export const zoomIn = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};

/**
 * 🎭 Flip In - Placeholder (sin animación)
 */
export const flipIn = (selector: string, options?: { delay?: number }) => {
  // Animación eliminada - usar CSS transitions si es necesario
  return { play: () => {}, pause: () => {} };
};
